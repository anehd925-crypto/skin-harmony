project_id = "kvhqhwsgqfxtuisgcfxn"

# analyze-ingredients: Safari JWT 호환성 문제로 false 유지 (클라이언트 invoke로만 호출됨)
[functions.analyze-ingredients]
verify_jwt = false

# scrape-oliveyoung은 공개 URL 스크래핑 — 로그인 불필요
[functions.scrape-oliveyoung]
verify_jwt = false

[functions.check-routine-conflicts]
verify_jwt = true

[functions.diary-insights]
verify_jwt = true

# pg_cron에서 직접 호출 — JWT 없이 실행됨
[functions.send-push-notification]
verify_jwt = false

# 서버 내부 자동화용
[functions.post-twitter-content]
verify_jwt = false

# 본인 인증 후 계정 삭제
[functions.delete-account]
verify_jwt = true

# 트러블 솔루션 — 로그인 없이도 사용 가능
[functions.skin-solution]
verify_jwt = false

# 날씨 기반 루틴 추천 — 로그인 없이도 사용 가능 (보관함은 클라이언트에서 조회 후 전달)
[functions.weather-routine]
verify_jwt = false

# AI 제품 검색 (텍스트 + vision) — 로그인 없이도 동작하도록 유지
[functions.product-search]
verify_jwt = false

# AI 제품 비교 — 프로필 없이도 기본 비교 가능
[functions.compare-products]
verify_jwt = false

# AI 피부 코치 — 클라이언트에서 데이터 수집 후 전달, JWT 불필요
[functions.skin-coach]
verify_jwt = false

# AI 피부 비서 대화 — 프로필/이력은 클라이언트가 전달
[functions.skin-chat]
verify_jwt = false
