# 채널 1. X(트위터) 자동 포스팅 — 완전 자동화 세팅 매뉴얼

> 설정 완료 후 매일 자동으로 콘텐츠가 생성·발행됩니다. 추가 작업 없음.

**자동화 수준**: ⭐⭐⭐⭐⭐ 완전 자동  
**초기 세팅 소요 시간**: 약 2~3시간 (1회만)  
**이후 운영 시간**: 주 1시간 (KPI 확인만)

---

## 전체 흐름 한눈에 보기

```
매일 오전 8시 (자동)
  ↓
Supabase Cron이 Edge Function 호출
  ↓
Edge Function: 요일별 콘텐츠 유형 결정
  ↓
Groq AI(Llama)가 포스팅 내용 자동 생성
  ↓
X API로 자동 트윗 발행
  ↓
결과(성공/실패) Supabase DB에 자동 저장
  ↓
매주 월요일: DB 확인하여 KPI 점검
```

---

## STEP 1. X(트위터) 개발자 계정 및 API 키 발급

### 1-1. X 마케팅 전용 계정 생성

> 개인 계정이 아닌 브랜드 전용 계정을 별도로 만드세요.

1. `x.com` 접속 → "계정 만들기" 클릭
2. 이름: `BeautyLens` (또는 `뷰티렌즈`)
3. 아이디(@): `BeautyLens_kr` 권장
4. 이메일: 브랜드 전용 이메일 사용 권장
5. 프로필 설정:
   - 프로필 사진: BeautyLens 로고 이미지
   - 헤더 이미지: 앱 스크린샷 또는 브랜드 이미지
   - 소개: `내 피부에 맞는 AI 전성분 분석 앱 | 올리브영 URL 붙여넣으면 바로 분석 | 무료 👇`
   - 웹사이트: BeautyLens 앱 URL (아래 단계에서 UTM 링크로 교체)

---

### 1-2. X Developer Portal에서 API 키 발급

> API란 앱끼리 서로 통신할 수 있게 해주는 열쇠입니다. 이 키가 있어야 자동 포스팅이 됩니다.

**① Developer Portal 접속**
1. 브라우저에서 `developer.twitter.com` 접속
2. 우측 상단 "Sign in" → 방금 만든 BeautyLens X 계정으로 로그인
3. 대시보드 화면이 나타나면 "Apply for access" 또는 "Get started" 클릭

**② 개발자 신청서 작성**
1. 사용 목적 질문이 나옵니다 → 아래처럼 작성

```
사용 목적 (영어로 작성 필요):
"I'm building an automated content marketing bot for my beauty app called BeautyLens.
The bot will post daily skincare ingredient information to educate users about cosmetic ingredients.
All content is original and educational. No scraping or data collection from other users."
```

2. "Will you make Twitter content available to a government entity?" → **No** 선택
3. 제출 후 승인까지 수 분~수일 소요 (대부분 즉시 승인)

**③ 앱 생성**
1. Developer Portal 좌측 메뉴 "Projects & Apps" → "Add App"
2. 앱 이름: `BeautyLens Marketing Bot`
3. 앱 설명: `Automated skincare content posting bot`
4. "Keys and tokens" 탭에서 아래 4가지 값을 복사

```
복사할 항목 (예시 형식):
─────────────────────────────────────────────
API Key:              abc123xyz...
API Key Secret:       def456uvw...
Access Token:         789ghi-aaa...
Access Token Secret:  jkl012bbb...
─────────────────────────────────────────────
⚠️ 이 값들은 화면에 한 번만 표시됩니다.
   반드시 메모장이나 노션에 즉시 복사 보관하세요.
```

**④ 쓰기 권한 설정** (중요 — 이것 빠뜨리면 포스팅 안 됨)
1. 앱 상세 페이지 → "Settings" 탭
2. "User authentication settings" → "Set up" 클릭
3. "App permissions": **Read and Write** 선택 (기본값이 Read Only이므로 반드시 변경)
4. "Type of App": **Web App, Automated App or Bot** 선택
5. "Callback URI": `https://qmlahovojjhkfmloifjc.supabase.co/auth/v1/callback`
6. "Website URL": 앱 URL 입력
7. "Save" 클릭

> ⚠️ 권한 변경 후 Access Token을 **재발급** 해야 합니다.  
> "Keys and Tokens" 탭 → "Regenerate" 클릭 → 새 토큰 다시 복사

---

## STEP 2. Supabase에 API 키 등록

> API 키를 코드에 직접 쓰면 외부에 노출될 수 있습니다. Supabase Secret에 안전하게 저장합니다.

1. `supabase.com` 접속 → BeautyLens 프로젝트 선택
2. 좌측 메뉴 → **"Edge Functions"** 클릭
3. 상단 탭 **"Secrets"** 클릭
4. "New secret" 버튼으로 아래 4개 항목을 **각각** 추가

| Secret 이름 (왼쪽 칸) | 값 (오른쪽 칸) |
|---------------------|--------------|
| `TWITTER_API_KEY` | Step 1에서 복사한 API Key |
| `TWITTER_API_SECRET` | Step 1에서 복사한 API Key Secret |
| `TWITTER_ACCESS_TOKEN` | Step 1에서 복사한 Access Token |
| `TWITTER_ACCESS_TOKEN_SECRET` | Step 1에서 복사한 Access Token Secret |

> `GROQ_API_KEY`는 이미 등록되어 있을 가능성이 높습니다. 없으면 추가하세요.

---

## STEP 3. 포스팅 로그 DB 테이블 생성

> 포스팅 성공/실패 기록을 저장하는 표를 만듭니다.

1. Supabase 대시보드 → 좌측 **"SQL Editor"** 클릭
2. "New query" 클릭
3. 아래 SQL을 전체 복사하여 붙여넣기
4. 우측 상단 **"Run"** 버튼 클릭

```sql
-- X 포스팅 로그 테이블 생성
CREATE TABLE IF NOT EXISTS twitter_post_logs (
  id              BIGSERIAL PRIMARY KEY,
  tweet_id        TEXT,
  content         TEXT,
  content_type    TEXT,           -- 'ingredient_info', 'conflict_warning', 'skin_type_tip', 'app_intro', 'trend'
  posted_at       TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  status          TEXT NOT NULL DEFAULT 'success',  -- 'success' | 'failed'
  error_message   TEXT,
  likes           INTEGER DEFAULT 0,
  retweets        INTEGER DEFAULT 0,
  replies         INTEGER DEFAULT 0,
  impressions     INTEGER DEFAULT 0,
  updated_at      TIMESTAMPTZ
);

-- 빠른 조회를 위한 인덱스
CREATE INDEX IF NOT EXISTS idx_twitter_logs_posted_at ON twitter_post_logs(posted_at DESC);
CREATE INDEX IF NOT EXISTS idx_twitter_logs_status    ON twitter_post_logs(status);

-- 확인 메시지
SELECT 'twitter_post_logs 테이블 생성 완료' AS result;
```

5. 하단에 "twitter_post_logs 테이블 생성 완료" 메시지가 나오면 성공

---

## STEP 4. Edge Function 파일 생성

> 자동 포스팅을 담당하는 코드 파일을 만듭니다.

터미널(Mac의 경우 터미널 앱)을 열고 아래 명령어를 순서대로 실행하세요.

### 4-1. 폴더 생성

```bash
# skin-harmony 프로젝트 폴더로 이동
cd /Users/anehd925/skin-harmony

# Edge Function 폴더 생성
mkdir -p supabase/functions/post-twitter-content
```

### 4-2. 코드 파일 생성

아래 명령으로 파일을 만들어도 되고, Cursor에서 직접 파일을 생성해도 됩니다.

**파일 경로**: `supabase/functions/post-twitter-content/index.ts`

**파일 내용** (전체 복사 후 파일에 저장):

```typescript
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

const corsHeaders = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Headers": "authorization, x-client-info, apikey, content-type",
};

// ────────────────────────────────────────────────────────────
// 요일·계절 정보 계산
// ────────────────────────────────────────────────────────────
function getContextInfo() {
  const now = new Date(new Date().toLocaleString("en-US", { timeZone: "Asia/Seoul" }));
  const dayNames = ["일요일", "월요일", "화요일", "수요일", "목요일", "금요일", "토요일"];
  const day = dayNames[now.getDay()];
  const month = now.getMonth() + 1;
  const season = month >= 3 && month <= 5 ? "봄" :
                 month >= 6 && month <= 8 ? "여름" :
                 month >= 9 && month <= 11 ? "가을" : "겨울";

  // 요일별 콘텐츠 유형 결정
  const typeMap: Record<number, { type: string; label: string }> = {
    0: { type: "skin_type_tip",    label: "피부 타입별 추천·주의 성분" },
    1: { type: "ingredient_info",  label: "오늘의 성분 정보 (효능·주의)" },
    2: { type: "conflict_warning", label: "함께 쓰면 안 되는 성분 조합 경고" },
    3: { type: "ingredient_info",  label: "오늘의 성분 정보 (효능·주의)" },
    4: { type: "app_intro",        label: "BeautyLens 앱 기능 시나리오 소개" },
    5: { type: "conflict_warning", label: "함께 쓰면 안 되는 성분 조합 경고" },
    6: { type: "trend",            label: `${season} 시즌 트렌드 성분 정보` },
  };

  return { day, season, ...typeMap[now.getDay()] };
}

// ────────────────────────────────────────────────────────────
// Groq AI로 트윗 내용 생성
// ────────────────────────────────────────────────────────────
async function generateTweet(contextType: string, contextLabel: string, season: string): Promise<string> {
  const appUrl = "https://beautylens.app?utm_source=twitter&utm_medium=organic&utm_campaign=daily_post";

  const typePrompts: Record<string, string> = {
    ingredient_info: `
오늘의 뷰티 성분 정보를 알려주는 트윗을 작성하세요.
- 특정 성분 1개를 선택 (나이아신아마이드, 레티놀, 세라마이드, 히알루론산, AHA/BHA, 비타민C, 판테놀, 아데노신, 녹차추출물, 알란토인 중 하나 선택)
- 효능 2가지, 주의사항 1가지 포함
- 어울리는 피부 타입 언급`,

    conflict_warning: `
화장품 성분 충돌 경고 트윗을 작성하세요.
- 함께 쓰면 효과가 줄거나 자극이 생기는 성분 조합 1가지
- 왜 문제인지 쉬운 설명
- 올바른 사용법 (대안) 제시
- 예시 조합: 레티놀+AHA, 비타민C+나이아신아마이드(고농도), 레티놀+비타민C, BHA+레티놀`,

    skin_type_tip: `
피부 타입별 성분 팁 트윗을 작성하세요.
- 건성/지성/복합성/민감성 중 하나 선택
- 해당 피부 타입이 피해야 할 성분 2가지
- 해당 피부 타입에 좋은 성분 2가지`,

    app_intro: `
BeautyLens 앱 기능을 자연스럽게 소개하는 트윗을 작성하세요.
- 직접 광고 금지, 실제 사용 시나리오 형태로
- "올리브영 URL을 붙여넣으면 내 피부 적합도를 분석해준다"는 점 중심
- 또는 "루틴 제품들의 성분 궁합을 한 번에 확인"하는 기능 소개`,

    trend: `
${season} 시즌에 주목받는 뷰티 성분 트렌드 트윗을 작성하세요.
- ${season}에 특히 중요한 성분 1~2가지
- 계절과의 연관성 설명 (예: 여름은 자외선 차단, 겨울은 보습 등)
- 사용 시 주의사항 1가지`,
  };

  const basePrompt = typePrompts[contextType] ?? typePrompts["ingredient_info"];

  const fullPrompt = `
당신은 뷰티·스킨케어 성분 전문 SNS 에디터입니다.
BeautyLens라는 AI 전성분 분석 앱의 공식 X(트위터) 계정 포스팅을 작성합니다.

[오늘의 콘텐츠 유형]
${basePrompt}

[반드시 지켜야 할 규칙]
1. 한국어 작성
2. 전체 230자 이내 (해시태그 포함)
3. 이모지 2~3개 사용
4. 마지막 줄: 앱 언급 1문장 + URL (${appUrl})
5. 해시태그 3~5개 (#스킨케어 #전성분 필수 포함)
6. 직접 광고 문구("다운로드하세요", "설치하세요") 금지
7. 정보성·유용성 중심

포스팅 내용만 출력. 다른 설명이나 부가 텍스트 없이.
`.trim();

  const res = await fetch("https://api.groq.com/openai/v1/chat/completions", {
    method: "POST",
    headers: {
      "Authorization": `Bearer ${Deno.env.get("GROQ_API_KEY")}`,
      "Content-Type": "application/json",
    },
    body: JSON.stringify({
      model: "llama-3.3-70b-versatile",
      messages: [{ role: "user", content: fullPrompt }],
      max_tokens: 400,
      temperature: 0.85,
    }),
  });

  if (!res.ok) throw new Error(`Groq API 오류: ${res.status}`);
  const data = await res.json();
  return data.choices[0].message.content.trim();
}

// ────────────────────────────────────────────────────────────
// X API OAuth 1.0a 서명 생성 (Deno 표준 라이브러리 사용)
// ────────────────────────────────────────────────────────────
async function buildOAuthHeader(method: string, url: string): Promise<string> {
  const consumerKey    = Deno.env.get("TWITTER_API_KEY")!;
  const consumerSecret = Deno.env.get("TWITTER_API_SECRET")!;
  const accessToken    = Deno.env.get("TWITTER_ACCESS_TOKEN")!;
  const tokenSecret    = Deno.env.get("TWITTER_ACCESS_TOKEN_SECRET")!;

  const timestamp = Math.floor(Date.now() / 1000).toString();
  const nonce = crypto.randomUUID().replace(/-/g, "");

  const oauthParams: Record<string, string> = {
    oauth_consumer_key:     consumerKey,
    oauth_nonce:            nonce,
    oauth_signature_method: "HMAC-SHA1",
    oauth_timestamp:        timestamp,
    oauth_token:            accessToken,
    oauth_version:          "1.0",
  };

  const allParams = { ...oauthParams };
  const paramString = Object.keys(allParams)
    .sort()
    .map(k => `${encodeURIComponent(k)}=${encodeURIComponent(allParams[k])}`)
    .join("&");

  const baseString = `${method}&${encodeURIComponent(url)}&${encodeURIComponent(paramString)}`;
  const signingKey = `${encodeURIComponent(consumerSecret)}&${encodeURIComponent(tokenSecret)}`;

  const keyData = await crypto.subtle.importKey(
    "raw",
    new TextEncoder().encode(signingKey),
    { name: "HMAC", hash: "SHA-1" },
    false,
    ["sign"]
  );
  const signatureBuffer = await crypto.subtle.sign(
    "HMAC",
    keyData,
    new TextEncoder().encode(baseString)
  );
  const signature = btoa(String.fromCharCode(...new Uint8Array(signatureBuffer)));

  oauthParams.oauth_signature = signature;

  const authHeader = "OAuth " + Object.keys(oauthParams)
    .sort()
    .map(k => `${encodeURIComponent(k)}="${encodeURIComponent(oauthParams[k])}"`)
    .join(", ");

  return authHeader;
}

// ────────────────────────────────────────────────────────────
// X에 트윗 게시
// ────────────────────────────────────────────────────────────
async function postTweet(text: string): Promise<string> {
  const url = "https://api.twitter.com/2/tweets";
  const authHeader = await buildOAuthHeader("POST", url);

  const res = await fetch(url, {
    method: "POST",
    headers: {
      "Authorization": authHeader,
      "Content-Type": "application/json",
    },
    body: JSON.stringify({ text }),
  });

  if (!res.ok) {
    const errBody = await res.text();
    throw new Error(`Twitter API 오류 ${res.status}: ${errBody}`);
  }

  const result = await res.json();
  return result.data.id as string;
}

// ────────────────────────────────────────────────────────────
// 메인 핸들러
// ────────────────────────────────────────────────────────────
Deno.serve(async (req) => {
  if (req.method === "OPTIONS") {
    return new Response("ok", { headers: corsHeaders });
  }

  const supabase = createClient(
    Deno.env.get("SUPABASE_URL")!,
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!
  );

  const ctx = getContextInfo();
  let tweetId: string | null = null;
  let tweetText: string | null = null;

  try {
    // 1. AI 콘텐츠 생성
    tweetText = await generateTweet(ctx.type, ctx.label, ctx.season);

    // 2. X에 포스팅
    tweetId = await postTweet(tweetText);

    // 3. 성공 로그 저장
    await supabase.from("twitter_post_logs").insert({
      tweet_id:     tweetId,
      content:      tweetText,
      content_type: ctx.type,
      status:       "success",
      posted_at:    new Date().toISOString(),
    });

    return new Response(
      JSON.stringify({ success: true, tweet_id: tweetId, content: tweetText }),
      { headers: { ...corsHeaders, "Content-Type": "application/json" } }
    );
  } catch (err) {
    const errorMessage = err instanceof Error ? err.message : String(err);
    console.error("포스팅 실패:", errorMessage);

    // 실패 로그 저장
    await supabase.from("twitter_post_logs").insert({
      tweet_id:      tweetId,
      content:       tweetText,
      content_type:  ctx.type,
      status:        "failed",
      error_message: errorMessage,
      posted_at:     new Date().toISOString(),
    });

    return new Response(
      JSON.stringify({ success: false, error: errorMessage }),
      { status: 500, headers: { ...corsHeaders, "Content-Type": "application/json" } }
    );
  }
});
```

---

## STEP 5. supabase/config.toml에 함수 등록

`supabase/config.toml` 파일 맨 아래에 아래 내용 추가:

```toml
[functions.post-twitter-content]
verify_jwt = false
```

---

## STEP 6. Edge Function 배포

터미널에서 아래 명령어 실행:

```bash
# skin-harmony 폴더에서 실행
cd /Users/anehd925/skin-harmony

# Supabase CLI 로그인 (처음 1회만)
npx supabase login

# Edge Function 배포
npx supabase functions deploy post-twitter-content --project-ref qmlahovojjhkfmloifjc

# 배포 완료 메시지 확인 후 수동 테스트 실행
npx supabase functions invoke post-twitter-content \
  --project-ref qmlahovojjhkfmloifjc \
  --body '{}'
```

**테스트 성공 시 응답 예시:**
```json
{
  "success": true,
  "tweet_id": "1234567890123456789",
  "content": "🧴 오늘의 성분: 나이아신아마이드\n\n..."
}
```

테스트 후 X 계정에 접속해서 실제로 트윗이 올라갔는지 확인하세요.

---

## STEP 7. 자동 스케줄(Cron) 등록

**파일 생성**: `supabase/migrations/20260416100000_add_twitter_cron.sql`

```sql
-- pg_cron, pg_net 확장 활성화
CREATE EXTENSION IF NOT EXISTS pg_cron;
CREATE EXTENSION IF NOT EXISTS pg_net;

-- 기존 스케줄 있으면 삭제 후 재등록
DO $$
BEGIN
  IF EXISTS (SELECT 1 FROM cron.job WHERE jobname = 'post-twitter-morning') THEN
    PERFORM cron.unschedule('post-twitter-morning');
  END IF;
  IF EXISTS (SELECT 1 FROM cron.job WHERE jobname = 'post-twitter-noon') THEN
    PERFORM cron.unschedule('post-twitter-noon');
  END IF;
END $$;

-- 매일 오전 8시 포스팅 (UTC 23:00 = KST 08:00)
SELECT cron.schedule(
  'post-twitter-morning',
  '0 23 * * *',
  $$
  SELECT net.http_post(
    url     := 'https://qmlahovojjhkfmloifjc.supabase.co/functions/v1/post-twitter-content',
    headers := jsonb_build_object(
      'Content-Type',  'application/json',
      'Authorization', 'Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InFtbGFob3Zvampoa2ZtbG9pZmpjIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzU3ODMzMjYsImV4cCI6MjA5MTM1OTMyNn0.a4mZFXkK3xjZYHib30_afE_uA6ok_GOwhiggKqS-rhw'
    ),
    body    := '{}'::jsonb
  );
  $$
);

-- 선택사항: 매일 낮 12시 추가 포스팅 (UTC 03:00 = KST 12:00)
-- 처음 1개월은 오전 1회만 운영 후 반응 보고 추가 여부 결정
-- SELECT cron.schedule('post-twitter-noon', '0 3 * * *', $$ ... $$);

SELECT 'Twitter 자동 포스팅 스케줄 등록 완료' AS result;
```

Supabase SQL Editor에서 위 SQL을 실행하세요.

---

## STEP 8. 마이그레이션 적용 (원격 Supabase에 반영)

```bash
cd /Users/anehd925/skin-harmony

# 원격 DB에 마이그레이션 적용
npx supabase db push --project-ref qmlahovojjhkfmloifjc
```

---

## STEP 9. 주간 KPI 확인 방법

매주 월요일 5분만 투자해 아래를 확인하세요.

**① Supabase에서 포스팅 성공/실패 확인**

Supabase → Table Editor → `twitter_post_logs` 테이블 클릭

| 확인 항목 | 정상 상태 |
|----------|----------|
| status = 'failed' 건수 | 주당 1건 이하 |
| 최근 7일 포스팅 수 | 7개 이상 |

**② X 계정에서 인게이지먼트 확인**

1. X 계정 로그인 → 프로필 우측 "더보기" → **"Analytics"** 클릭
2. 확인 지표:
   - 노출수(Impressions): 꾸준히 증가하는지
   - 참여율(Engagement rate): 1% 이상 유지되는지
   - 프로필 방문 수: 증가 추세인지

**③ 앱 유입 확인**

Supabase → `users` 또는 `auth.users` 테이블에서  
`raw_app_meta_data` 컬럼의 UTM 파라미터 확인 (Google Analytics 연동 시 더 편함)

---

## 문제 해결 가이드

| 증상 | 원인 | 해결 방법 |
|------|------|----------|
| 포스팅이 전혀 안 됨 | Cron 미등록 또는 Secret 누락 | SQL Editor에서 `SELECT * FROM cron.job` 실행해 스케줄 확인 |
| `401 Unauthorized` 오류 | Access Token 권한 부족 | X Developer Portal에서 Read+Write 권한 재확인, 토큰 재발급 |
| `403 Forbidden` 오류 | 무료 플랜 포스팅 한도 초과 | X API Free tier는 월 1,500건 제한 (하루 2회면 월 60건이라 문제없음) |
| AI 생성 내용이 이상함 | 프롬프트 품질 문제 | `index.ts`의 typePrompts 내용 수정 후 재배포 |
| 같은 내용이 반복됨 | temperature 값이 낮음 | 코드에서 `temperature: 0.85` → `1.0`으로 변경 |

---
